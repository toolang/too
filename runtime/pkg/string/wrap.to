use runtime

func new(init<i8*>){
	r<i8*> = stringnew(init)
	return runtime.newobject(runtime.STRING,r)
}
func sub(v<runtime.Value>,lo){
	str<i8*> = v.data
	l<i32> = *lo
	if l > stringlen(str) {
		return ""
	}

	str += l
	return new(str)
}